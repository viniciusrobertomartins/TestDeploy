name: Test working directory1

on:
  push:
    branches: 
      - master
    paths:
      - 'dir1/**'    

defaults:
    run: 
        working-directory: ./dir1 
  
jobs:
    job1:
        runs-on: ubuntu-16.04
        env: 
          GITHUB_TOKEN: ${{ seecrets.GITHUB_TOKEN }} 
        
        steps:
        - name: Check commit status
          uses: actions/checkout@v2
          with:
            fetch-depth: 0  

        - uses: actions/checkout@master
        - name: dir1 directory
          run: cat README.md           
       
         